import { HighlightSearchDirective } from './highlight-search.directive';

describe('HighlightSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightSearchDirective();
    expect(directive).toBeTruthy();
  });
});
